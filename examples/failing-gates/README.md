# Focused Failing Gates

Each child fixture contains one deliberate defect and declares the single expected failing check.
They are intentionally not complete investigations and exist for onboarding and regression tests.
