# Single Explore Domain

Single defect: both otherwise valid sources use one reserved synthetic domain. The expected failure
is only `source_triangulation`; link checks remain skipped because evaluation is offline.
