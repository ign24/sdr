# Complete Light Example

This source fixture demonstrates `intake -> explore -> transfer -> reuse -> done`. All names and
results are invented. Explore uses contextual citations to reserved domains and therefore carries
no redistributed snapshots. The runner always advances with `offline=True`.
